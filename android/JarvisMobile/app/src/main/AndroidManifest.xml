<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />
    <!-- Opcionais: solicitadas somente ao usar a funcionalidade correspondente. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />

    <uses-permission
        android:name="android.permission.NEARBY_WIFI_DEVICES"
        android:usesPermissionFlags="neverForLocation" />

    <!-- BLE e opcional e usado somente no provisionamento da ESP32-CAM. -->
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" android:usesPermissionFlags="neverForLocation" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_jarvis_launcher"
        android:roundIcon="@drawable/ic_jarvis_launcher"
        android:label="JARVIS Mobile"
        android:theme="@style/Theme.JarvisMobile"
        android:usesCleartextTraffic="false">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Painel operacional independente: exige sessao local valida e retorna ao login se necessario. -->
        <activity
            android:name=".DashboardActivity"
            android:exported="true"
            android:label="CASA Dashboard"
            android:theme="@style/Theme.JarvisMobile">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".NewDevicesActivity"
            android:exported="false"
            android:theme="@style/Theme.JarvisMobile" />

        <activity
            android:name=".WatchSetupActivity"
            android:exported="false"
            android:theme="@style/Theme.JarvisMobile" />

        <activity
            android:name=".WatchCameraActivity"
            android:exported="false"
            android:theme="@style/Theme.JarvisMobile" />

        <service
            android:name=".JarvisConnectionService"
            android:exported="false"
            android:foregroundServiceType="dataSync|connectedDevice" />

    </application>
</manifest>
